<Page x:Class="CateringEquipmentApp.Pages.UsersPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="Transparent">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.Resources>
            <Style TargetType="DataGrid">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="GridLinesVisibility" Value="None"/>
                <Setter Property="HeadersVisibility" Value="Column"/>
                <Setter Property="RowHeaderWidth" Value="0"/>
                <Setter Property="CanUserAddRows" Value="False"/>
                <Setter Property="CanUserDeleteRows" Value="False"/>
                <Setter Property="CanUserResizeRows" Value="False"/>
                <Setter Property="SelectionMode" Value="Single"/>
                <Setter Property="SelectionUnit" Value="FullRow"/>
                <Setter Property="AlternatingRowBackground" Value="{DynamicResource AppTableAltRowBrush}"/>
                <Setter Property="RowBackground" Value="White"/>
                <Setter Property="FontSize" Value="14"/>
            </Style>

            <Style TargetType="DataGridColumnHeader">
                <Setter Property="Background" Value="{DynamicResource AppTableHeaderBrush}"/>
                <Setter Property="Foreground" Value="{DynamicResource AppTextPrimaryBrush}"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="FontSize" Value="13"/>
                <Setter Property="Padding" Value="14,12"/>
                <Setter Property="Height" Value="46"/>
                <Setter Property="BorderBrush" Value="{DynamicResource AppCardBorderBrush}"/>
                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                <Setter Property="HorizontalContentAlignment" Value="Left"/>
            </Style>

            <Style TargetType="DataGridCell">
                <Setter Property="Padding" Value="12,10"/>
                <Setter Property="BorderBrush" Value="#E8EEF4"/>
                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                <Setter Property="Foreground" Value="{DynamicResource AppTextPrimaryBrush}"/>
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="DataGridCell">
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}">
                                <ContentPresenter VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Style.Triggers>
                    <Trigger Property="IsSelected" Value="True">
                        <Setter Property="Background" Value="{DynamicResource AppTableSelectedBrush}"/>
                        <Setter Property="Foreground" Value="#0F4360"/>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <Style TargetType="DataGridRow">
                <Setter Property="MinHeight" Value="42"/>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="{DynamicResource AppTableHoverBrush}"/>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <Style x:Key="SoftTextBoxStyle" TargetType="TextBox">
                <Setter Property="Height" Value="44"/>
                <Setter Property="Padding" Value="16,8"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="Foreground" Value="{DynamicResource AppTextPrimaryBrush}"/>
                <Setter Property="Background" Value="{DynamicResource AppControlBackgroundBrush}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource AppControlBorderBrush}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="TextBox">
                            <Border x:Name="SearchBorder"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="16">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Viewbox Width="14"
                                             Height="14"
                                             Margin="14,0,12,0"
                                             VerticalAlignment="Center">
                                        <Canvas Width="16" Height="16">
                                            <Ellipse Width="9" Height="9" Stroke="#6F8799" StrokeThickness="1.8"/>
                                            <Line X1="8.5" Y1="8.5" X2="14" Y2="14" Stroke="#6F8799" StrokeThickness="1.8" StrokeStartLineCap="Round" StrokeEndLineCap="Round"/>
                                        </Canvas>
                                    </Viewbox>
                                    <ScrollViewer Grid.Column="1"
                                                  x:Name="PART_ContentHost"
                                                  Margin="0,0,14,0"
                                                  VerticalAlignment="Center"/>
                                </Grid>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="SearchBorder" Property="Background" Value="#FBFDFF"/>
                                    <Setter TargetName="SearchBorder" Property="BorderBrush" Value="#BFD3E2"/>
                                </Trigger>
                                <Trigger Property="IsKeyboardFocused" Value="True">
                                    <Setter TargetName="SearchBorder" Property="Background" Value="White"/>
                                    <Setter TargetName="SearchBorder" Property="BorderBrush" Value="{DynamicResource AppControlFocusBrush}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="SoftComboStyle" TargetType="ComboBox">
                <Setter Property="Height" Value="44"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="Foreground" Value="{DynamicResource AppTextPrimaryBrush}"/>
                <Setter Property="Background" Value="{DynamicResource AppControlBackgroundBrush}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource AppControlBorderBrush}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Padding" Value="16,8"/>
                <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
                <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ComboBox">
                            <Grid>
                                <Border x:Name="ComboBorder"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="16">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="46"/>
                                        </Grid.ColumnDefinitions>

                                        <ContentPresenter Margin="16,0,12,0"
                                                          VerticalAlignment="Center"
                                                          HorizontalAlignment="Left"
                                                          RecognizesAccessKey="True"
                                                          Content="{TemplateBinding SelectionBoxItem}"
                                                          ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"/>

                                        <ToggleButton Grid.Column="1"
                                                      x:Name="DropDownToggle"
                                                      Focusable="False"
                                                      IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                                      Background="Transparent"
                                                      BorderBrush="Transparent"
                                                      BorderThickness="0"
                                                      Cursor="Hand">
                                            <ToggleButton.Template>
                                                <ControlTemplate TargetType="ToggleButton">
                                                    <Border Background="Transparent">
                                                        <Path x:Name="ArrowPath"
                                                              Width="12"
                                                              Height="7"
                                                              HorizontalAlignment="Center"
                                                              VerticalAlignment="Center"
                                                              Fill="#5A778D"
                                                              Data="M 0 0 L 12 0 L 6 7 Z"
                                                              RenderTransformOrigin="0.5,0.5">
                                                            <Path.RenderTransform>
                                                                <RotateTransform Angle="0"/>
                                                            </Path.RenderTransform>
                                                        </Path>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsChecked" Value="True">
                                                            <Trigger.EnterActions>
                                                                <BeginStoryboard>
                                                                    <Storyboard>
                                                                        <DoubleAnimation Storyboard.TargetName="ArrowPath"
                                                                                         Storyboard.TargetProperty="RenderTransform.(RotateTransform.Angle)"
                                                                                         To="180"
                                                                                         Duration="0:0:0.18"/>
                                                                    </Storyboard>
                                                                </BeginStoryboard>
                                                            </Trigger.EnterActions>
                                                            <Trigger.ExitActions>
                                                                <BeginStoryboard>
                                                                    <Storyboard>
                                                                        <DoubleAnimation Storyboard.TargetName="ArrowPath"
                                                                                         Storyboard.TargetProperty="RenderTransform.(RotateTransform.Angle)"
                                                                                         To="0"
                                                                                         Duration="0:0:0.18"/>
                                                                    </Storyboard>
                                                                </BeginStoryboard>
                                                            </Trigger.ExitActions>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </ToggleButton.Template>
                                        </ToggleButton>
                                    </Grid>
                                </Border>

                                <Popup x:Name="PART_Popup"
                                       Placement="Bottom"
                                       Margin="0,6,0,0"
                                       AllowsTransparency="True"
                                       Focusable="False"
                                       PopupAnimation="Fade"
                                       IsOpen="{TemplateBinding IsDropDownOpen}">
                                    <Border MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}"
                                            MaxHeight="280"
                                            Background="White"
                                            BorderBrush="{DynamicResource AppControlBorderBrush}"
                                            BorderThickness="1"
                                            CornerRadius="16">
                                        <ScrollViewer SnapsToDevicePixels="True">
                                            <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained"/>
                                        </ScrollViewer>
                                    </Border>
                                </Popup>
                            </Grid>

                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="ComboBorder" Property="Background" Value="#FBFDFF"/>
                                    <Setter TargetName="ComboBorder" Property="BorderBrush" Value="#BFD3E2"/>
                                </Trigger>
                                <Trigger SourceName="DropDownToggle" Property="IsMouseOver" Value="True">
                                    <Setter TargetName="ComboBorder" Property="Background" Value="#FBFDFF"/>
                                    <Setter TargetName="ComboBorder" Property="BorderBrush" Value="#BFD3E2"/>
                                </Trigger>
                                <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                    <Setter TargetName="ComboBorder" Property="Background" Value="White"/>
                                    <Setter TargetName="ComboBorder" Property="BorderBrush" Value="{DynamicResource AppControlFocusBrush}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style TargetType="ComboBoxItem">
                <Setter Property="Foreground" Value="{DynamicResource AppTextPrimaryBrush}"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="Padding" Value="14,10"/>
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ComboBoxItem">
                            <Border x:Name="ItemBorder" Background="{TemplateBinding Background}" CornerRadius="10">
                                <ContentPresenter Margin="{TemplateBinding Padding}"
                                                  VerticalAlignment="Center"
                                                  HorizontalAlignment="Left"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsHighlighted" Value="True">
                                    <Setter TargetName="ItemBorder" Property="Background" Value="#EFF6FB"/>
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter TargetName="ItemBorder" Property="Background" Value="#DDF0FB"/>
                                    <Setter Property="Foreground" Value="#0F4360"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </Grid.Resources>

        <Border Style="{StaticResource CardStyle}" Margin="0,0,0,18">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel>
                        <TextBlock Text="Администрирование"
                                   Foreground="{DynamicResource AppTextMutedBrush}"
                                   FontSize="13"
                                   FontWeight="SemiBold"/>
                        <TextBlock Text="Управление пользователями"
                                   Margin="0,8,0,0"
                                   FontSize="30"
                                   FontWeight="Bold"
                                   FontFamily="Bahnschrift"
                                   Foreground="{DynamicResource AppTextPrimaryBrush}"/>
                        <TextBlock Text="Сотрудники системы, роли доступа и контактные данные в одном рабочем разделе."
                                   Margin="0,10,0,0"
                                   Foreground="{DynamicResource AppTextSecondaryBrush}"
                                   FontSize="15"
                                   TextWrapping="Wrap"/>

                        <WrapPanel Margin="0,14,0,0" ItemHeight="36">
                            <Border Background="#E8F3FA" CornerRadius="14" Padding="12,6" Margin="0,0,10,10" VerticalAlignment="Center">
                                <TextBlock Text="Просмотр" Foreground="#1C5573" FontWeight="SemiBold"/>
                            </Border>
                            <Border Background="#EFF7E8" CornerRadius="14" Padding="12,6" Margin="0,0,10,10" VerticalAlignment="Center">
                                <TextBlock Text="Добавление" Foreground="#3C6B28" FontWeight="SemiBold"/>
                            </Border>
                            <Border Background="#FFF4DF" CornerRadius="14" Padding="12,6" Margin="0,0,10,10" VerticalAlignment="Center">
                                <TextBlock Text="Редактирование" Foreground="#8A5A08" FontWeight="SemiBold"/>
                            </Border>
                            <Border Background="#FDEAE7" CornerRadius="14" Padding="12,6" Margin="0,0,10,10" VerticalAlignment="Center">
                                <TextBlock Text="Удаление" Foreground="#A33B32" FontWeight="SemiBold"/>
                            </Border>
                        </WrapPanel>
                    </StackPanel>

                    <WrapPanel Grid.Column="1" HorizontalAlignment="Right">
                        <Button x:Name="AddButton" Style="{StaticResource ActionButtonStyle}" Click="AddButton_Click">
                            <StackPanel Orientation="Horizontal">
                                <Image Source="{StaticResource AddIcon}" Width="16" Height="16" Margin="0,0,8,0"/>
                                <TextBlock Text="Добавить" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                        <Button x:Name="EditButton" Style="{StaticResource ActionButtonStyle}" Background="#D7962C" Click="EditButton_Click">
                            <StackPanel Orientation="Horizontal">
                                <Image Source="{StaticResource EditIcon}" Width="16" Height="16" Margin="0,0,8,0"/>
                                <TextBlock Text="Редактировать" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                        <Button x:Name="DeleteButton" Style="{StaticResource ActionButtonStyle}" Background="#D85B50" Margin="0" Click="DeleteButton_Click">
                            <StackPanel Orientation="Horizontal">
                                <Image Source="{StaticResource DeleteIcon}" Width="16" Height="16" Margin="0,0,8,0"/>
                                <TextBlock Text="Удалить" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                    </WrapPanel>
                </Grid>
            </Grid>
        </Border>

        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Margin="0,0,0,14">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2.4*"/>
                        <ColumnDefinition Width="1.2*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBox x:Name="SearchTextBox"
                             Grid.Column="0"
                             Style="{StaticResource SoftTextBoxStyle}"
                             TextChanged="SearchTextBox_TextChanged"
                             ToolTip="Поиск по таблице пользователей"/>

                    <ComboBox x:Name="ColumnFilterComboBox"
                              Grid.Column="1"
                              Margin="12,0,12,0"
                              Style="{StaticResource SoftComboStyle}"
                              SelectionChanged="ColumnFilterComboBox_SelectionChanged"/>

                    <Button x:Name="ResetFilterButton"
                            Grid.Column="2"
                            Style="{StaticResource ActionButtonStyle}"
                            Margin="0"
                            Click="ResetFilterButton_Click">
                        <StackPanel Orientation="Horizontal">
                            <Image Source="{StaticResource ResetIcon}" Width="16" Height="16" Margin="0,0,8,0"/>
                            <TextBlock Text="Сбросить" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </Grid>

                <Grid Grid.Row="1">
                    <DataGrid x:Name="UsersGrid"
                              AutoGenerateColumns="True"
                              IsReadOnly="True"
                              ColumnHeaderHeight="46"
                              RowHeaderWidth="0"
                              FontSize="14"/>

                    <Border x:Name="EmptyStatePanel"
                            Background="#FAFCFE"
                            CornerRadius="22"
                            BorderBrush="{DynamicResource AppCardBorderBrush}"
                            BorderThickness="1"
                            Padding="28"
                            Visibility="Collapsed">
                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                            <Border Width="58"
                                    Height="58"
                                    CornerRadius="18"
                                    Background="#E8F3FA"
                                    HorizontalAlignment="Center">
                                <Image Source="{StaticResource KitchenCareIcon}" Width="26" Height="26" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                            </Border>
                            <TextBlock Text="Пользователи пока не найдены"
                                       Margin="0,14,0,0"
                                       FontSize="21"
                                       FontWeight="SemiBold"
                                       FontFamily="Bahnschrift"
                                       Foreground="{DynamicResource AppTextPrimaryBrush}"
                                       HorizontalAlignment="Center"/>
                            <TextBlock Text="Добавьте первую учетную запись через верхнюю панель действий."
                                       Margin="0,10,0,0"
                                       FontSize="14"
                                       Foreground="{DynamicResource AppTextSecondaryBrush}"
                                       TextAlignment="Center"
                                       TextWrapping="Wrap"
                                       MaxWidth="420"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Page>
